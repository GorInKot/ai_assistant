# План-роадмап: работа с документами

**Дата составления:** 2026-06-15
**Контекст:** Этап 5 роадмапа закрыт (см. [RoadMap05.md](RoadMap05.md)). Новое направление — обработка пользовательских документов внутри ассистента.

**Конечная цель:** пользователь загружает в чат файл (Excel/Word), а ассистент умеет извлекать из него данные, обрабатывать (выжимка, объединение, сравнение) и возвращать результат.

---

## Этап 6. Работа с документами

### 6.A. Excel (`.xlsx`/`.xls`)

| # | Возможность | Описание |
|---|---|---|
| 6.A.1 | Извлечение данных | Разобрать загруженный файл: листы, таблицы, заголовки, строки. Вернуть структурированное представление. |
| 6.A.2 | Объединение нескольких файлов | Слить несколько небольших файлов в один. **Предусловие:** проверка идентичности структуры таблиц (совпадение набора/порядка/типов колонок). При расхождении — сообщить пользователю, что именно не совпало, и не объединять «вслепую». |
| 6.A.3 | Поиск различий | Сравнить два (или более) файла и показать различия: добавленные/удалённые строки, изменённые ячейки, расхождения в структуре. |

### 6.B. Word (`.docx`)

| # | Возможность | Описание |
|---|---|---|
| 6.B.1 | Извлечение данных | Разобрать загруженный файл: текст, заголовки, абзацы, таблицы внутри документа. |
| 6.B.2 | Краткая выжимка | По большому тексту вернуть краткое резюме (summary) через LLM. |
| 6.B.3 | Сравнение файлов | Сравнить два файла одного типа и найти различия (изменения в тексте, добавленные/удалённые фрагменты). |

---

## Открытые вопросы (уточнить перед реализацией)

1. **Загрузка файлов.** Через чат (вложение к сообщению) или отдельная ручка/UI? Где хранить загруженный файл — временно на время сессии или в БД?
2. **Лимиты.** Максимальный размер файла, число строк/листов, число одновременно объединяемых/сравниваемых файлов.
3. **Формат вывода различий.** Текстовый отчёт в чате, скачиваемый файл (diff/новый xlsx), или и то и другое?
4. **Excel-объединение «идентичная структура».** Насколько строгая проверка: только имена колонок, или ещё порядок и типы? Что делать с разным регистром/пробелами в заголовках?
5. **Word-сравнение.** Diff по словам/предложениям/абзацам? Учитывать ли форматирование или только текст?
6. **Выжимка Word.** Длина резюме, язык, использовать ли существующий LLM-конфиг (Groq/YC).

---

## Предварительные технические заметки

- **Excel:** `openpyxl` уже заложен в роадмапе (Этап 4, импорт сотрудников) — переиспользовать. Для табличных операций удобен `pandas`.
- **Word:** `python-docx` для чтения `.docx`. Старый `.doc` (бинарный) — отдельный вопрос, возможно вне скоупа первой итерации.
- **Выжимка:** переиспользовать существующий LLM-вызов из `app/llm.py`.
- **Архитектура:** вынести в отдельный модуль/сервис (напр. `app/services/documents.py`) + роутер; не раздувать существующие маршруты.

---

## Очерёдность

1. Excel: извлечение (6.A.1) → объединение (6.A.2) → различия (6.A.3).
2. Word: извлечение (6.B.1) → выжимка (6.B.2) → сравнение (6.B.3).

Начинаем с Excel.

---

## Этап 7. Автозаполнение шаблонов заявок на доступ к системам

**Цель:** пользователь произвольным текстом передаёт свои данные, ассистент сам понимает, какие данные для чего нужны, заполняет шаблон заявки и выдаёт пользователю готовый заполненный документ.

> **Источник механики:** пользователь предоставил рабочие VBA-макросы автозаполнения (`VBA/*.txt`) для 4 систем — **Лаборатория ИИ, ЦУС, Терминальный сервер, ВКД**. По ним воспроизводим логику в ассистенте.

### Прогресс

| Система | Статус | Способ |
|---|---|---|
| **Лаборатория ИИ** | ✅ готово (2026-06-19) | заполнение готового шаблона `.xlsx` |
| ЦУС | ⏳ запланировано | строится с нуля кодом |
| Терминальный сервер | ⏳ запланировано | строится с нуля кодом |
| ВКД | ⏳ запланировано | строится с нуля кодом |

### Сделано: Лаборатория ИИ (✅)

**Что получили:** пользователь в чате свободным текстом описывает данные (сотрудник, руководитель, подразделение, ключ ПКЗИ) и просит «оформи/заполни заявку на Лабораторию ИИ». Ассистент распознаёт намерение, через LLM извлекает поля, заполняет бланк `Шаблон_Лаборатория.xlsx` и отдаёт готовый `Заявка_<ФИО>.xlsx` на скачивание. Если обязательных данных не хватает — не выдумывает, а перечисляет недостающее и просит дописать.

**Решённые развилки (договорённости с пользователем):**
- **Ввод** — свободный текст в чате (а не загрузка «Базы» Excel, как в макросе).
- **Шаблон Лаборатории** — предоставлен пользователем (`Шаблон_Лаборатория.xlsx`), хранится в `app/form_templates/lab_ai.xlsx`.
- **Маппинг полей** — по макросу `LaboratoryAI_3_1`: подразделение → F15/H47; «ФИО, должность, телефон» работника → F16; то же по руководителю → F17; ключ ПКЗИ → F18; дата (текущая, период «по» = год+1) → F31/H31/I31/K31/M31/N31; ФИО внизу → B43 (работник)/B50 (руководитель).
- **Выдача** — готовый файл + текстовая сводка распознанных полей в чате.
- **Объём** — пока один сотрудник за запрос (многих — позже).

**Технические заметки / грабли:**
- Детект интента консервативный: нужен И глагол («оформи/заполни/сформируй/…»), И название системы — иначе обычные вопросы про лабораторию не перехватываются. Встроено в `/api/ask` шагом 0, не мешает заявкам Этапа 5 и QA.
- Файлы обрабатываются в памяти, на сервере не сохраняются (как в Этапе 6).
- **Совместимость с Apple Numbers:** openpyxl при `load+save` пересобирает пакет в свой диалект (теряет `printerSettings`, `worksheets/_rels`, пишет inline-строки вместо `sharedStrings`). Excel/Google Sheets открывают, **Numbers — нет**. Решение: патчим исходный Excel-пакет напрямую (меняем только нужные ячейки и `sharedStrings`, остальное байт-в-байт). Подтверждено пользователем — открывается в Numbers.
- **Скачивание на фронте:** перевели вложения на Blob/object-URL вместо `data:`-URI (для бинарных `.xlsx` data-URI повреждал байты). Заодно чинит скачивание `merged.xlsx` из Этапа 6.

**Файлы:** `app/services/request_forms.py` (реестр систем, детект, заполнение, патч пакета), `app/llm.py::extract_fields`, `app/services/ask_service.py` (шаг 0), `app/form_templates/lab_ai.xlsx`, `frontend/src/chat/MessageList.tsx` (Blob-скачивание), `tests/test_request_forms.py` (10 тестов).

### Системы (шаблоны загрузит пользователь позже)
- **Лаборатория ИИ** ✅
- **ЦУС** (макрос строит лист с нуля)
- **Терминальный сервер** (макрос строит лист с нуля)
- **ВКД** (макрос строит лист с нуля)

### Что нужно реализовать

| # | Возможность | Описание |
|---|---|---|
| 7.1 | Загрузка/хранение шаблонов | Пользователь загрузит шаблоны заявок (формат уточнить — вероятно `.docx`/`.xlsx`). Хранить и выбирать по типу системы. |
| 7.2 | Произвольный ввод данных | Формат ввода пользователя — свободный текст. Ассистент автоматически определяет, какие данные предоставлены и для какого поля заявки нужны (механика будет уточнена пользователем позже). |
| 7.3 | Маппинг данных в поля шаблона | Куда какие данные вставлять — пользователь объяснит при работе над функционалом. Заложить конфигурируемое сопоставление «извлечённое поле → место в шаблоне». |
| 7.4 | Заполнение шаблона | Подставить распознанные данные в выбранный шаблон. |
| 7.5 | Выдача результата | Вернуть пользователю заполненную заявку (формат вывода уточнить — готовый файл и/или превью в чате). |

### Ожидает уточнений от пользователя
- Конкретные шаблоны заявок (ЦУС / ЕКТП / Лаборатория ИИ).
- Механика автоопределения «какие данные для чего нужны».
- Правила маппинга: какие данные в какие поля шаблона вставлять.
- Формат вывода заполненной заявки.

---

## Мои вопросы пользователю (ждут ответа)

> Записаны из обсуждения в чате — пользователь ответит позже.

**По документам (Этап 6) — ОТВЕТЫ ПОЛУЧЕНЫ (2026-06-23):**
1. **Загрузка файлов.** ✅ Через чат — вложение к сообщению, как у Claude/DeepSeek/ChatGPT. (Уже реализовано в Этапе 6: файлы в памяти, на сервере не сохраняются.)
2. **Лимиты — размер файла.** ✅ Разные по типу (xlsx тяжелее в RAM из-за openpyxl): **`.xlsx` — 15 МБ, `.docx` — 30 МБ**. Сейчас единый `MAX_FILE_SIZE = 10 МБ` → нужно развести по типам.
3. **Формат вывода различий.** ✅ Текстовый отчёт в чате. (Уже так в Этапе 6.)
4. **Excel — «идентичная структура».** ✅ Сверяем **только имена колонок; порядок может отличаться**. ⚠️ Сейчас сравнение order-sensitive (`document_processing.py:247`) → переписать на сравнение по множеству имён + автоперестановку колонок к единому порядку при объединении.
5. **Word-сравнение.** ✅ По тексту (форматирование не учитываем). (Уже так в Этапе 6.)
6. **Выжимка Word.** ✅ Пропорциональное правило `≈ clamp(объём × ~10%, мин, макс)` с диапазонами: короткий (<300 слов) → 2-3 предложения; средний (300-2000) → абзац ~100-150 слов; длинный (2000-10000) → буллеты ~200-300 слов; очень длинный (>30k симв.) → разбивка→свод ~300-400 слов. Язык — русский. Передать целевую длину в `summarize_text` (сейчас зовётся без длины).
7. **Старый `.doc`** (бинарный, не `.docx`) — ✅ **РЕАЛИЗОВАНО (2026-06-23).** Поддержан через внешний конвертер: `antiword` на Linux/прод (ставится в Dockerfile), `textutil` на macOS/dev (встроен). Извлекается только текст — без таблиц/форматирования (осознанное упрощение: все Word-операции у нас текстовые, а `.doc` — легаси). Конвертация во временном каталоге, удаляется сразу (stateless сохранён). Файлы: `document_processing._doc_to_text`/`_parse_doc_legacy`/`parse_docx`, `chat_documents._word`, `Composer.tsx` (accept), `Dockerfile` (antiword), 6 тестов в `test_chat_documents.py`. End-to-end проверено на настоящем бинарном `.doc`.

**Доработки по Этапу 6 (под полученные ответы) — ✅ СДЕЛАНО (2026-06-23):**
- ✅ Разнесён лимит размера: `MAX_FILE_SIZE_XLSX = 15 МБ` / `MAX_FILE_SIZE_WORD = 30 МБ` (Excel строже — openpyxl тяжёл в RAM). Хелпер `_check_size`.
- ✅ `merge_workbooks`: сравнение колонок по *набору* нормализованных имён (регистр/пробелы игнорируются), порядок свободный — строки автоматически переставляются к порядку первого файла. При расхождении сообщает, каких колонок не хватает / какие лишние. Дубли имён → явный отказ.
- ✅ `summarize_text`: пропорциональная длина выжимки (`_summary_length_instruction`) по диапазонам слов: <300 → 2-3 предлож.; <2000 → ~100-150 слов; <10000 → ~200-300 слов списком; иначе ~300-400 слов.
- Тесты: +9 в `test_chat_documents.py` (лимиты, переупорядочивание/регистр/недостающие колонки, шкала длины выжимки). Всего по Этапу 6 — 61 тест зелёный.

**Сравнение бланков-форм (6.A.3 для заявок) — ✅ СДЕЛАНО (2026-06-23):**
- Проблема: заявки Этапа 7 — это *формы* (пары «подпись — значение», объединённые ячейки), а `diff_workbooks` рассчитан на *таблицы-реестры*. На заявках он выдавал нечитаемое («[строка 3] ООО "РН-СтройКонтроль"»), хотя само отличие находил.
- Решение: эвристика `is_form_like` (разрежённость листа: `density < FORM_DENSITY_THRESHOLD=0.30`) разводит форму и таблицу. Для формы — `diff_forms`: сравнение ячеек по координатам, каждое отличие подписано ближайшей текстовой ячейкой слева/сверху (у бланков подпись в колонке B) + координата. Реестры по-прежнему идут через `diff_workbooks`.
- Теперь ответ читается: «**Ф.И.О. работника, должность, телефон (F16)**: «…» → «…, 89123451234»».
- Файлы: `document_processing.py` (`SheetGrid`/`parse_xlsx_grid`/`is_form_like`/`diff_forms`/`_nearest_label`), `chat_documents.py` (`_diff_two_xlsx`/`_answer_diff_form`). +3 теста. Всего Этап 6 — 64 теста зелёные. Проверено на реальном шаблоне `lab_ai.xlsx`.

**Атрибуция различий по документу (UX всех diff-режимов) — ✅ СДЕЛАНО (2026-06-23):**
- По просьбе пользователя: каждое отличие подписывается документом, в котором оно есть. Формат: «**Только в документе «2»:** строка «000»».
- Применено единообразно к трём режимам: Word (`_answer_diff_docx`), Excel-реестры (`_answer_diff_xlsx`), Excel-бланки (`_answer_diff_form`). Группы: «Изменённые …», «Только в документе «{new}»» (добавленное), «Только в документе «{old}»» (удалённое). Имена — реальные имена файлов.
- +2 теста (атрибуция строк реестра и абзацев Word). Всего Этап 6 — 66 тестов зелёные.

**По автозаполнению заявок (Этап 7):**
8. В каком формате будут шаблоны заявок (`.docx` / `.xlsx` / другое)?
9. Как именно должно работать автоопределение «какие данные для чего нужны»?
10. Правила маппинга данных в поля шаблона.
11. Формат выдачи заполненной заявки (файл / превью в чате / оба).
